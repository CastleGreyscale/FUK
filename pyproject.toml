[project]
name = "fuk"
version = "0.2.0"
description = "Framework for Unified Kreation - Professional VFX AI Pipeline"
requires-python = ">=3.10"
dependencies = [
    # Core Deep Learning
    "torch>=2.7.0",
    "torchvision>=0.22.0",
    "xformers>=0.0.31",
    "triton>=3.1.0",
    "torchaudio>=2.7.0",
    # INT8 attention for the Wan DiT. Deliberately NOT accompanied by
    # flash-attn: FA2 outranks sage-attn in DiffSynth's backend chain, so
    # installing it would silently take sage-attn out of play.
    "sageattention>=1.0.6",

    # Hugging Face Ecosystem
    "transformers>=4.56.0",
    "diffusers>=0.32.0",
    "accelerate>=1.6.0",
    "safetensors>=0.4.5",
    "huggingface-hub>=0.34.0",
    "timm>=1.0.0",
    
    # Preprocessors & Control
    "controlnet_aux>=0.0.10",
    "geffnet>=1.0.2",

    # SeedVR2 video restoration (fuk/vendor/SeedVR2). The engine itself is
    # vendored, but these two are not otherwise pulled in: gguf reads the
    # quantized DiT weights, rotary_embedding_torch backs its rope.
    "gguf>=0.19.0",
    "rotary_embedding_torch>=0.5.3",
    
    # Image/Video Processing
    "opencv-python>=4.10.0",
    "pillow>=11.0.0",
    "OpenEXR>=3.4.0",
    "imageio>=2.37.0",
    "imageio-ffmpeg>=0.6.0",
    "av>=14.0.0",
    "scikit-image>=0.25.0",
    
    # Upscaling
    "basicsr>=1.4.2",
    
    # Scientific Computing
    "numpy>=1.26.0,<2.0.0",
    "scipy>=1.15.0",
    "einops>=0.7.0",
    
    # Web Framework
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "python-multipart>=0.0.21",
    "starlette>=0.50.0",
    "websockets>=11.0.0",
    "tk>=0.1.0",
    
    # Utilities
    "pyyaml>=6.0.0",
    "tqdm>=4.67.0",
    "omegaconf>=2.3.0",
    "requests>=2.32.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.14.0",
]

# Depth Anything V3 specific dependencies
# Install with: pip install -e ".[depth]"
depth = [
    "moviepy==1.0.3",
    "typer>=0.9.0",
    "pycolmap",
    "plyfile",
    "e3nn",
    "open3d",
    "trimesh",
    "pillow_heif",
]

[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["fuk"]

[tool.ruff]
line-length = 100
target-version = "py310"
